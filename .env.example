# Environment configuration for chatbot application

# n8n Configuration
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http
N8N_WEBHOOK_URL=http://localhost:5678
N8N_EDITOR_BASE_URL=http://localhost:5678
GENERIC_TIMEZONE=Europe/Berlin

# PostgreSQL Configuration for n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=changeme
POSTGRES_DB=n8n

# PostgreSQL Configuration for chatbot-backend
DB_HOST=postgres-chatbot
DB_PORT=5432
DB_NAME=chatbot_db
DB_USER=chatbot_user
DB_PASSWORD=chatbot_password

# Backend Configuration
BACKEND_PORT=8080
SPRING_PROFILES_ACTIVE=prod

# Frontend Configuration
FRONTEND_PORT=3000
VITE_API_BASE_URL=http://localhost:8080

# Ollama Configuration
OLLAMA_PORT=11434

# LiteLLM Configuration
LITELLM_PORT=4000
LITELLM_UI_USERNAME=admin
LITELLM_UI_PASSWORD=admin
LITELLM_MASTER_KEY=sk-local-master

# OpenAI/LiteLLM Configuration
OPENAI_BASE_URL=http://litellm:4000
OPENAI_API_KEY=sk-local-master

# MCP Configuration
MCP_ENCRYPTION_KEY=prod-32-character-secret-key-here-change-me

# ============================================================================
# SSL/TLS Certificate Configuration (for Corporate Proxies like Zscaler)
# ============================================================================
#
# OPTIONAL: Only needed if you're behind a corporate SSL-intercepting proxy
# Leave commented out or empty if you're not in a corporate environment
#
# If you see errors like "certificate signed by unknown authority" when
# Ollama tries to pull models, you need to configure this.

# Path to the corporate root CA certificate (e.g., Zscaler)
# This file should be in PEM format (.crt or .pem)
# Export it from your browser or system certificate store
# Place it in the ./certs/ directory
#
# IMPORTANT: If this file doesn't exist, containers will start normally
# using system default certificates (works fine for non-corporate environments)
#ZSCALER_ROOT_CA_PATH=./certs/zscaler-root-ca.crt

# Optional: Additional CA certificates (comma-separated paths)
#ADDITIONAL_CA_CERTS_PATH=

# Enable/disable SSL certificate verification (use 'true' for production)
#SSL_VERIFY_ENABLED=true

# Note: The following environment variables are set automatically in docker-compose:
# - REQUESTS_CA_BUNDLE (Python requests library)
# - SSL_CERT_FILE (Python SSL, curl)
# - PIP_CERT (Python pip package manager)
# - NODE_EXTRA_CA_CERTS (Node.js HTTPS)
# - NPM_CONFIG_CAFILE (npm package manager)
# - CURL_CA_BUNDLE (curl command)
